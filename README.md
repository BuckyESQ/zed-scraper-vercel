# Zed-Scraper-Vercel

Serverless Puppeteer scraper for pulling NFT asset URLs from an OpenSea wallet page.

## Usage
- Deploy to Vercel with `/api/scrape.js` as a function.
- Hit `/api/scrape?url=<WALLET_URL>&go=true` to trigger the scrape.
